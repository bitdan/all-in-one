<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linger.module.groupbuy.infrastructure.mapper.GroupBuyDelayTaskMapper">

    <select id="claimBatch" resultType="com.linger.module.groupbuy.infrastructure.entity.GroupBuyDelayTaskEntity">
        WITH candidates AS (
            SELECT id
            FROM groupbuy_delay_tasks
            WHERE (status = 'PENDING'
                OR (status IN ('CLAIMED', 'RUNNING') AND locked_until &lt; NOW()))
              AND execute_at &lt;= NOW() + (INTERVAL '1 second' * #{loadAheadSeconds})
            ORDER BY execute_at
            LIMIT #{limit}
            FOR UPDATE SKIP LOCKED
        )
        UPDATE groupbuy_delay_tasks t
        SET status = 'CLAIMED',
            worker_id = #{workerId},
            updated_by = 0,
            locked_until = NOW() + (INTERVAL '1 second' * #{lockSeconds}),
            updated_at = NOW()
        FROM candidates c
        WHERE t.id = c.id
        RETURNING t.*
    </select>

    <insert id="insertIgnore">
        INSERT INTO groupbuy_delay_tasks(task_type, business_id, execute_at)
        VALUES (#{taskType}, #{businessId}, #{executeAt})
        ON CONFLICT (task_type, business_id) DO NOTHING
    </insert>

    <update id="markRunning">
        UPDATE groupbuy_delay_tasks
        SET status = 'RUNNING',
            updated_by = 0,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'CLAIMED'
          AND worker_id = #{workerId}
          AND locked_until > NOW()
    </update>

    <update id="markDone">
        UPDATE groupbuy_delay_tasks
        SET status = 'DONE',
            worker_id = NULL,
            locked_until = NULL,
            updated_by = 0,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'RUNNING'
          AND worker_id = #{workerId}
    </update>

    <update id="markFailed">
        UPDATE groupbuy_delay_tasks
        SET status = CASE
                WHEN retry_count + 1 >= #{maxRetries} THEN 'DEAD'
                ELSE 'PENDING'
            END,
            retry_count = retry_count + 1,
            execute_at = NOW() + (INTERVAL '1 second' * #{delaySeconds}),
            last_error = #{error},
            worker_id = NULL,
            locked_until = NULL,
            updated_by = 0,
            updated_at = NOW()
        WHERE id = #{id}
          AND worker_id = #{workerId}
    </update>
</mapper>
