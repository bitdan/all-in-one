<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linger.module.groupbuy.transaction.mapper.GroupBuyMemberMapper">

    <update id="markPaid">
        UPDATE groupbuy_members
        SET status = 'PAID', updated_by = 0, updated_at = NOW()
        WHERE order_id = #{orderId} AND status = 'RESERVED'
    </update>

    <update id="markConfirmedByGroup">
        UPDATE groupbuy_members
        SET status = 'CONFIRMED', updated_by = 0, updated_at = NOW()
        WHERE group_id = #{groupId} AND status = 'PAID'
    </update>

    <update id="cancelReservation">
        UPDATE groupbuy_members
        SET status = 'CANCELLED', updated_by = 0, updated_at = NOW()
        WHERE order_id = #{orderId} AND status = 'RESERVED'
    </update>

    <update id="markRefundingByGroup">
        UPDATE groupbuy_members
        SET status = 'REFUNDING', updated_by = 0, updated_at = NOW()
        WHERE group_id = #{groupId} AND status = 'PAID'
    </update>

    <update id="markRefunding">
        UPDATE groupbuy_members
        SET status = 'REFUNDING', updated_by = 0, updated_at = NOW()
        WHERE order_id = #{orderId} AND status IN ('RESERVED', 'PAID')
    </update>

    <update id="markRefunded">
        UPDATE groupbuy_members
        SET status = 'REFUNDED', updated_by = 0, updated_at = NOW()
        WHERE order_id = #{orderId} AND status = 'REFUNDING'
    </update>
</mapper>
