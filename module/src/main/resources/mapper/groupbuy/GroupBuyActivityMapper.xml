<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linger.module.groupbuy.activity.mapper.GroupBuyActivityMapper">

    <select id="selectForUpdate" resultType="com.linger.module.groupbuy.activity.entity.GroupBuyActivityEntity">
        SELECT *
        FROM groupbuy_activities
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <update id="markRunning">
        UPDATE groupbuy_activities
        SET status = 'RUNNING',
            version = version + 1,
            updated_by = 0,
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 'READY'
    </update>
</mapper>
