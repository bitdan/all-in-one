spring:
  # Spring Data Redis 配置
  data:
    redis:
      repositories:
        enabled: false # 项目未使用到 Spring Data Redis 的 Repository，所以直接禁用，保证启动速度
  redis:
    host: ${REDIS_HOST:43.156.83.246} # 地址
    password: ${REDIS_PASSWORD:dudu0.0@}
    port: ${REDIS_PORT:6379} # 端口
    database: ${REDIS_DATABASE:15} # 数据库索引
    redisson:
      file: classpath:redisson-local.yml

mybatis-plus:
  configuration:
    # 为所有 Mapper 日志增加统一前缀，便于只在本地开启 SQL、参数和结果打印。
    log-prefix: mybatis.sql.

#logging:
#  level:
#    # MyBatis 使用 Mapper statement id 作为 logger 名称。
#    mybatis.sql: DEBUG
#    # RedisTemplate 可能使用 Lettuce；RedissonClient 和当前 RedisTemplate 使用 Redisson。
#    io.lettuce.core.protocol.CommandHandler: DEBUG
#    org.redisson.command.RedisExecutor: DEBUG
